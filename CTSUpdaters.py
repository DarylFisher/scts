
import Mapping
import ImportEntity, BusinessEntity, EntityColumn, EntityDefinition



# Takes an import Entity and a mapping and build the business Entity 
def processUpdates(m,ie):

    be = BusinessEntity("")

# do default fields 
    for df in m.mappingDefaults:
        c = EntityColumn(df.ColumnName)


# Map the fields


# Perform the function maps
    


# Concats a list of strings to one
def functionConcat(l):
    c = ""
    for s in l 
        c += S
    return c

